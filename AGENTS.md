# PaperX Agent Instructions

Before working on PaperX:

1. Read `PROJECT_CONTEXT.md`.
2. If `graphify-out/graph.json` exists, use `graphify query` for architecture and dependency questions before broad manual searching.
3. Preserve unrelated user changes.

Notion documentation rule:

- The PaperX root page is `https://app.notion.com/p/3ad947a88a00803a91f8ff66b8a5845a`.
- Create feature learning notes as child pages under that root only when the user explicitly asks.
- Never create or update Notion pages automatically after implementation.
- Notes should be beginner-friendly but technically complete, including purpose, mental model, flow, files, decisions, security, edge cases, verification, limitations, and next steps.

